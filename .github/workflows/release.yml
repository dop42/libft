name: CI/CD

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build-linux:
    name: Build Linux utility library
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build utility library
        run: make re

  release-linux:
    name: Publish Linux utility library release
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build-linux
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build release artifact
        run: make re

      - name: Create Linux release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v1.0.${{ github.run_number }}-linux
          name: Release v1.0.${{ github.run_number }}-linux
          files: |
            utility_library.a
            includes/utility_library.h
